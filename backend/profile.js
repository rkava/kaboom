 /**
  * backend/playerdata.js
  * 
  * Returns all information used on the profile page
  */
module.exports = function( req, res ) { 

	//all the data in the database about the player on 
	//the profile, and a comparison with the user if 
	//they're logged in
    
}